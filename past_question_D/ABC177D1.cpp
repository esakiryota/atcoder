#include <stdio.h>
#include<iostream>
#include <vector>
#include <set>
#include <map>
#include <iomanip>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
const int INF = 1001001001;
typedef long long ll;

int dfs(vector<vector<int> > v, int a) {
  rep(i,m) {
    
  }
}

int main() {
  int n,m;
  cin >> n >> m;
  vector<vector<int> > v(m, vector<int>(2));
  rep(i,m) {
    cin >> v[i][0] >> v[i][1];
  }

}
