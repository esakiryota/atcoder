#include <stdio.h>
#include<iostream>
#include <vector>
#include <set>
#include <queue>
#include <map>
#include <iomanip>
#include <bitset>
#include <math.h>
#define rep(i,n) for (int i = 0; i < (n); i++)
using namespace std;
const int INF = 1001001001;
typedef long long ll;
typedef pair<int, int> P;

vector<ll> v(200);
vector<int> w(n);
vector<int> jud(n, false);
vector<int> ansb;
vector<int> ansc;
int m = 0;
jud[0]

int dfs(int now, int sum) {
  for (int i = 0; i < n; i++) {
    if ()
  }
}

int main() {
  int n;
  cin >> n;

  int mod = 200;
  rep(i,n) {
    int a;
    cin >> a;
    int b = a%mod;
    w[i] = b;
  }

  rep(i, 201) {

  }
}
